import { World } from "prismarine-world/types/world";

export * as iterators from "prismarine-world/types/iterators";

// Make public the types related to World, but not the classes
// (since they aren't exported in js)
import type * as world from "prismarine-world/types/world";
export { world };

export default function loader(mcVersion: string): typeof World;
